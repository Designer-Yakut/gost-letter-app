# 🖋️ Тренажёр чертежного шрифта по ГОСТ

Веб-приложение для генерации обучающих шаблонов букв и цифр с соблюдением стандарта ГОСТ 2.304 (ISO 3098).

Позволяет создавать SVG, PNG, PDF и GIF-анимации, визуализирующие порядок написания букв.  
Полезно для учеников, педагогов, родителей, методистов и инженеров при оформлении титульных листов и обучении техническому письму.
---
## 🎯 Назначение

Программа позволяет:

- обучать техническому письму и каллиграфии;
- формировать тренировочные и тестовые листы;
- генерировать титульные страницы с произвольным текстом по ГОСТ (в PDF/PNG/SVG);
- визуализировать порядок написания букв (GIF-анимация);
- работать как в режиме обучения, так и в режиме тестирования (режим «только точки»);
- применять набор различных шрифтов ГОСТ (включая A, B, наклонные варианты);
- расширять коллекцию шрифтов пользователем (поддержка .ttf и .otf);
- использовать в браузере без установки на клиенте.
---
## 🚀 Возможности

- ✅ Поддержка верхнего и нижнего регистра
- ✅ Загрузка текста из `.txt` файла или ввода вручную
- ✅ Выбор шрифта из папки `fonts/`
- ✅ Показ сетки, букв, точек — в любых комбинациях
- ✅ Поддержка ГОСТ и классической (вертикальной) сетки
- ✅ Изменение:
  - интервала между словами;
  - высоты букв `h` (мм);
  - толщины линий `d` (вручную или авто: `d = h / 14`);
  - шага горизонтальных и вертикальных линий;
- ✅ Цветовая настройка: рамка, сетка, буквы
- ✅ Автоматическая генерация имени выходной папки
- ✅ Режим обучения (`training_real`) — буквы из текста, без знаков
- ✅ Режим тестирования — "только точки" (по началу слов)
Потенциальные применения:
•	Обучение каллиграфии;
•	Тренажёры рукописного ввода;
•	Подготовка графических тетрадей;
•	Визуализация нестандартных шрифтов (например, арабских, иероглифов);
•	Генерация специализированных бланков/форм.
---
## 🧪 Форматы экспорта

| Формат | Назначение                                                                 |
|--------|----------------------------------------------------------------------------|
| PDF    | Для печати и создания методических материалов                             |
| PNG    | Высокое разрешение, подходит для печати и демонстраций                    |
| SVG    | Векторный экспорт, редактируем в редакторах (например Inkscape)          |
| GIF    | Пошаговая анимация написания букв (если включён `training_real`)         |
---
## 🌐 Интерфейс

Интерфейс — это одностраничная веб-форма, содержащая:

- поля для ввода текста;
- настройки высоты букв, толщины линий, интервалов, шагов сетки;
- чекбоксы для включения/отключения: сетки, букв, точек, режима "тест", "классическая сетка";
- выпадающий список для выбора шрифта;
- цветовые поля: рамка, сетка, буквы/точки;
- загрузка текстового файла;
- кнопка генерации PDF/PNG/SVG/GIF.

*Поддержка серой палитры (0.6 / 0.4 / 0.6) для подготовки обучающих материалов с пониженной насыщенностью.*

## 🧩 Структура проекта (актуальная)

```
gost_letter_app/                    # Корневая директория
├── app.py                          # Flask-приложение, основной сервер
├── Font_on_TEMP5_to_GOST.py       # Модуль отрисовки текста, сетки и точек
├── fonts/                          # Папка со шрифтами (.ttf, .otf)
│   ├── gost_type_a.ttf
│   ├── gost_type_b.ttf
│   └── ...
├── static/                         # Статические файлы (картинки, gif, демо)
│   └── demo/
│       └── eva_story_01.gif        # Пример анимации
├── templates/                      # Шаблоны Jinja2 (если используются)
├── requirements.txt               # Зависимости проекта
├── README.md                       # Этот файл
└── .gitignore                      # Игнорируемые файлы Git
```

---

## 💾 Запуск

### 📦 Установка зависимостей

```bash
pip install -r requirements.txt
```

### 🚀 Запуск

```bash
python app.py
```

После запуска приложение будет доступно по адресу:  
[http://127.0.0.1:5000](http://127.0.0.1:5000)

## ☁️ Онлайн-версия

[https://gost-letter-app.onrender.com](https://gost-letter-app.onrender.com)

_⚠️ На бесплатных инстансах Render (512MB памяти) некоторые функции, включая SVG-генерацию и большие тексты, могут быть отключены._


## 🛠️ Примечания

- Для тестов можно отключить сетку и текст, оставив только точки — ученик должен вписать текст вручную.
- Программа создаёт директорию `output_ГГГГ-ММ-ДД_имяпользователя` для сохранения всех экспортов.
- Все параметры поддаются ручной настройке — удобно для разных этапов обучения.


## 📷 Пример интерфейса

![Интерфейс](static/demo/eva_story_01.gif)

Интерфейс представляет собой компактную одностраничную веб-форму, содержащую:

Поле ввода текста — для ручного ввода символов, в данной версии данная функция ограничена до 2-х строк.
неограниченная функция временно не поддерживается на Render (*малые инстансы, 512MB памяти для неоплачиваемого тарифа)

Загрузку текстового файла — чтобы быстро подгрузить задание*.

Настройки генерации:

1) интервал;
2) размер букв (высота прописных букв - основной параметр, от которого могут стандартно зависить: 
интервал, тощина линий шрифта, шаг сетки);
3) толщина линий;
4) шаг горизонтальных линий;
5) шаг вертикальных линий;
6) поля выбора цвета (рамка, сетка, буквы);
7) выбор мтодики обучения и тестирования (по уолчанию - "Показать сетку", "Показать буквы", 
допускается отдельно отключить или выбрать любую из операций, при выборе операции "Только точки", 
сетка и буквы отключаются автоматически (предназначено для тестирования);
8) классическая сетка - сетка без наклона;
9) выбор форматов вывода (многострочный список);
10) кнопка «Сгенерировать»;
11) кнопки "Просмотреть README.md"  "Смотреть презентацию"- открытие непосредственно в браузере;
12) ссылки на скачивание PDF/PNG/SVG/GIF после генерации, для скачивания GIF - указать, 
*скачивание SVG - функция временно не поддерживается на Render (*малые инстансы, 512MB памяти для неоплачиваемого тарифа);

# 📌 Особенности интерфейса (UI)

| Элемент                                       | Тип HTML              | Особенности реализации                                                                                                                             
|--------------------------------------------|-------------------------|----------------------------------------------------------------------------------------                                                                              
| Интервал между словами <input type="number">  Автоматически рассчитывается при изменении высоты шрифта (0.42 * h)   
| Высота прописных букв (h) <input type="number">  Базовый параметр. Влияет на шаг сетки и толщину линий  
| Стандартная толщина линий <input type="checkbox"> При включении блокирует поле толщины, устанавливая d = h/14                                          
| Толщина линий шрифта (d) <input type="number"> Влияет на отрисовку текста и сетки, обновляется автоматически или задаётся вручную    
| Шаг горизонтальных линий <input type="number"> По умолчанию равен толщине линии                                                                                              
| Шаг вертикальных линий<input type="number"> |По умолчанию равен толщине линии                                                                                          
| Цвет рамки / сетки / букв<select> |Выпадающие списки с предопределёнными цветами                                                         
| Использовать серую палитру <input type="checkbox"> При активации задаёт цвета: рамка 0.6, сетка 0.4, буквы 0.6                                               
| Показать сетку <input type="checkbox"> Устанавливает отрисовку горизонтальных и вертикальных линий                                           
| Показать буквы <input type="checkbox"> Включает отображение текста                                                                                                       
| Только точки <input type="checkbox"> Показывает только точки начала слов (тестирование), отключает сетку и текст                  
| Классическая сетка <input type="checkbox"> Прямая вертикальная сетка без наклона                                                                                         
| Выбор шрифта <select> Генерируется из содержимого папки /fonts, по умолчанию выбран ГОСТ тип A (наклонный)   
| Ввод текста <textarea> Поддержка многострочного ввода, используется если не загружен файл                                  
| Загрузка текстового файла <input type="file"> txt-файл, приоритетнее поля ввода                                                                                                  
| Режим обучения <input type="checkbox"> Используется для генерации training_images.gif из букв, исключая знаки                                
| Кнопка генерации <input type="submit"> Отправляет форму на сервер, инициирует генерацию PDF/PNG/SVG/GIF                                
| Ссылка на README.md <a href="/readme"> Открывает файл README.md прямо в браузере (рендерится как HTML)                                  
| Ссылки на презентацию и скачивание <a href="..." download> Появляются только после генерации (GIF после флагирования), SVG временно не работает  

🧩 Дополнительно

HTML-код формы внедрён напрямую в Python через render_template_string, без внешнего шаблона.

Динамика (JavaScript):

1) при изменении высоты h автоматически пересчитываются: d, spacing, шаги сетки (функция произвольного ввода также остается);
2) при включении «серой палитры» цвета обновляются мгновенно.

В демо- версии многое ограничено, в т.ч. SVG не работает временно на Render из-за ограничений бесплатного инстанса (512MB). 
Существует полная версия (запуск через батник или ехе-файл)

📃 Лицензия

MIT License (в перспективе - проприетарная лицензия)

© Mike Yakutsenak 2025 | Авторская разработка | GOST letter trainer